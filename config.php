<?php

declare(strict_types=1);

/**
 * This website is provided free of charge.
 * Author: Powerpuff — https://powerpuff.pro/
 * Discord: https://discord.gg/QwCsWtP99A
 * GitHub: https://github.com/PowerpuffIO
 */

return [
    /*
     * ---------------------------------------------------------------------------
     * URL / EN: Public URL path segment when the site lives in a subdirectory (e.g. `/wow`). Empty = site at domain root.
     * RU: Префикс URL, если сайт в подпапке (например `/wow`). Пусто — сайт в корне домена.
     * ---------------------------------------------------------------------------
     */
    'url_prefix' => '',
    /*
     * ---------------------------------------------------------------------------
     * EN: Same as url_prefix for link generation; usually matches deployment path.
     * RU: То же для внутренних ссылок; обычно совпадает с каталогом установки.
     * ---------------------------------------------------------------------------
     */
    'base_path' => '',

    /*
     * -----------------------------------------------------------------------
     * EN: Site database (CMS: users, news, settings, vote log).
     * RU: База сайта (CMS: пользователи сайта, новости, настройки, лог голосов).
     * -----------------------------------------------------------------------
     */
    'site_db' => [
        'host' => '127.0.0.1',
        'port' => 3306,
        'name' => 'argast_site',
        'user' => 'root',
        'pass' => '',
        'charset' => 'utf8mb4',
    ],

    /*
     * -----------------------------------------------------------------------
     * EN: WoW auth / realmd database (accounts, realmlist).
     * RU: Игровая база auth / realmd (аккаунты, realmlist).
     * -----------------------------------------------------------------------
     */
    'auth_db' => [
        'host' => '127.0.0.1',
        'port' => 3306,
        'name' => 'auth',
        'user' => 'root',
        'pass' => '',
        'charset' => 'utf8mb4',
    ],

    /*
     * -----------------------------------------------------------------------
     * EN: Characters database (realm statistics, online count).
     * RU: База персонажей (статистика реалма, онлайн).
     * -----------------------------------------------------------------------
     */
    'characters_db' => [
        'host' => '127.0.0.1',
        'port' => 3306,
        'name' => 'characters',
        'user' => 'root',
        'pass' => '',
        'charset' => 'utf8mb4',
    ],

    /*
     * -----------------------------------------------------------------------
     * EN: PHP session cookie name (unique per site if multiple apps on one domain).
     * RU: Имя cookie сессии PHP (уникальное, если на домене несколько приложений).
     * -----------------------------------------------------------------------
     */
    'session_name' => 'argastwww',

    /*
     * -----------------------------------------------------------------------
     * EN: Filesystem path for uploaded images (must be writable by the web server).
     * RU: Путь на диске для загрузок (должен быть доступен на запись для веб-сервера).
     * -----------------------------------------------------------------------
     */
    'upload_dir' => __DIR__ . '/storage/uploads',

    /*
     * -----------------------------------------------------------------------
     * EN: Public URL path prefix for uploaded files.
     * RU: URL-префикс для отдачи загруженных файлов.
     * -----------------------------------------------------------------------
     */
    'upload_url' => '/storage/uploads',

    /*
     * -----------------------------------------------------------------------
     * EN: Default language: `ru` or `en` (visitors can switch via `?lang=`).
     * RU: Язык по умолчанию: `ru` или `en` (переключение через `?lang=`).
     * -----------------------------------------------------------------------
     */
    'default_lang' => 'ru',

    /*
     * -----------------------------------------------------------------------
     * EN: Game expansion flag for account insert (`account.expansion`): 2 = WotLK typical.
     * RU: Расширение для поля `expansion` при создании аккаунта: 2 = обычно WotLK.
     * -----------------------------------------------------------------------
     */
    'wow_expansion' => 2,

    /*
     * -----------------------------------------------------------------------
     * EN: If true, registration may expose raw DB errors (debug only).
     * RU: Если true, при регистрации возможны подробные ошибки БД (только отладка).
     * -----------------------------------------------------------------------
     */
    'debug_auth_errors' => false,

    /*
     * -----------------------------------------------------------------------
     * EN: Bot protection on login / register / forgot-password.
     *     0 = disabled. 1 = Google reCAPTCHA v2 (“I’m not a robot”).
     *     2 = Cloudflare Turnstile.
     *     Set site + secret keys below.
     * RU: Защита от ботов на входе, регистрации и восстановлении пароля.
     *     0 = выкл. 1 = Google reCAPTCHA v2. 2 = Cloudflare Turnstile.
     *     Укажите ключи ниже.
     * -----------------------------------------------------------------------
     */
    'captcha_type' => 0,

    /*
     * -----------------------------------------------------------------------
     * EN: Public site key . RU: Публичный ключ .
     * -----------------------------------------------------------------------
     */
    'captcha_site_key' => '',

    /*
     * -----------------------------------------------------------------------
     * EN: Secret key. RU: Секретный ключ .
     * -----------------------------------------------------------------------
     */
    'captcha_secret_key' => '',

    /*
     * -----------------------------------------------------------------------
     * EN: reCAPTCHA UI language (`hl`), e.g. `en`, `ru`. Turnstile auto-detects; this is ignored for type 2.
     * RU: Язык виджета reCAPTCHA (`hl`: `en`, `ru`). Для Turnstile не используется.
     * -----------------------------------------------------------------------
     */
    'captcha_language' => 'en',
];
